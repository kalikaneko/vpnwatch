# vpnwatch

```
❯ ./vpnwatch -h
Usage of ./vpnwatch:
  -ban-log string
        [BAN_LOG] Ban log file (default "/tmp/openvpn-banlist")
  -status-log string
        [STATUS_LOG] Status log file (default "/tmp/openvpn-status-tcp")
```
